import React from "react";

const Post = ({
  key,
  postId,
  postUserId,
  name,
  description,
  location,
  picturePath,
  userPicturePath,
  likes,
  comments,
}) => {
  return <div>Post</div>;
};

export default Post;
